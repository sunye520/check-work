<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yogee.youge.modules.check.dao.CheckApproverDeployDao">
    
	<sql id="checkApproverDeployColumns">
		a.id AS "id",
		a.create_date AS "createDate",
		a.update_date AS "updateDate",
		a.del_flag AS "delFlag",
		a.type AS "type",
		a.department_id AS "departmentId",
		a.approver_id AS "approverId",
		a.approver_type AS "approverType",
		a.cc_id AS "ccId",
		a.time AS "time"
	</sql>
	
	<sql id="checkApproverDeployJoins">
	</sql>
    
	<select id="get" resultType="CheckApproverDeploy">
		SELECT 
			<include refid="checkApproverDeployColumns"/>
		FROM check_approver_deploy a
		<include refid="checkApproverDeployJoins"/>
		WHERE a.id = #{id}
	</select>
	
	<select id="findList" resultType="CheckApproverDeploy">
		SELECT 
			<include refid="checkApproverDeployColumns"/>
		FROM check_approver_deploy a
		<include refid="checkApproverDeployJoins"/>
		<where>
			a.del_flag = #{DEL_FLAG_NORMAL}
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
				ORDER BY a.update_date DESC
			</otherwise>
		</choose>
	</select>
	
	<select id="findAllList" resultType="CheckApproverDeploy">
		SELECT 
			<include refid="checkApproverDeployColumns"/>
		FROM check_approver_deploy a
		<include refid="checkApproverDeployJoins"/>
		<where>
			a.del_flag = #{DEL_FLAG_NORMAL}
		</where>		
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
				ORDER BY a.update_date DESC
			</otherwise>
		</choose>
	</select>
	
	<insert id="insert">
		INSERT INTO check_approver_deploy(
			id,
			create_date,
			update_date,
			del_flag,
			type,
			department_id,
			approver_id,
			approver_type,
			cc_id,
			time
		) VALUES (
			#{id},
			#{createDate},
			#{updateDate},
			#{delFlag},
			#{type},
			#{departmentId},
			#{approverId},
			#{approverType},
			#{ccId},
			#{time}
		)
	</insert>
	
	<update id="update">
		UPDATE check_approver_deploy SET 	
			update_date = #{updateDate},
			type = #{type},
			department_id = #{departmentId},
			approver_id = #{approverId},
			approver_type = #{approverType},
			cc_id = #{ccId},
			time = #{time}
		WHERE id = #{id}
	</update>
	
	<update id="delete">
		UPDATE check_approver_deploy SET 
			del_flag = #{DEL_FLAG_DELETE}
		WHERE id = #{id}
	</update>
	
</mapper>